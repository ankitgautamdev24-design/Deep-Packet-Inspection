# Deep-Packet-Inspection
A C++-based Deep Packet Inspection Engine that reads PCAP files, identifies applications through TLS SNI extraction, tracks network flows, applies filtering rules, and generates traffic analysis reports.
